{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "factus": {
      "type": "local",
      "command": ["uv", "run", "mcp", "run", "main.py"],
      "enabled": true,
      "timeout": 30000
    }
  }
}
